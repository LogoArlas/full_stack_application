//constructer to create User objects
function User(userid, fName, lName, uName, passwd) {
    this.userId=userid
    this.fisrtName=fName
    this.lastName=lName
    this.username=uName
    this.password=passwd
}